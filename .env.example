# Database Configuration
DATABASE_URL=127.0.0.1:8000

# JWT Secret Key (change in production)
JWT_SECRET=your-secret-key-change-me

# Encryption Key for storing credentials (must be 32 characters)
ENCRYPTION_KEY=change-me-to-32-byte-secret!!

# Server Configuration
HOST=127.0.0.1
PORT=8080

# Logging
RUST_LOG=info
